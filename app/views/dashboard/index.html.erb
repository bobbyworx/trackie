<div class="page-header">
    <h1> Welcome to Trackie, <%= link_to "#{current_user.username}", edit_user_registration_path %></span></h1>
</div></br></br>

<h2>
<p><%= "What do you want to do today?" %></br></br></p>
<p> Your last added income is: <span style="color:green"> <%= "#{Income.last.try(:amount)} RON " %> </span> in <%= "#{Income.last.try(:income_type).try(:name)}" %> -
  <%= link_to "add new", new_income_path %> Income </br></br></p>

<p> Your last added expense is: <span style="color:red"> <%= "#{Expense.last.try(:amount)} RON " %></span> in <%= "#{Expense.last.try(:expense_type).try(:name)}" %> -
  <%= link_to "add new", new_expense_path %> Expese </br></br></p>
<p> Your balance for this month is: <span style="color:green"> 
  <%= Income.where("created_at BETWEEN ? AND ?", DateTime.now.beginning_of_month, DateTime.now).sum(:amount) - 
           Expense.where("created_at BETWEEN ? AND ?", DateTime.now.beginning_of_month, DateTime.now).sum(:amount) "RON" %></span></p>
</h2>