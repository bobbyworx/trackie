<div class="page-header">
    <h1> Welcome to Trackie, <%= link_to "#{current_user.username}", edit_user_registration_path %></span></h1>
</div></br></br>

<h2>
<p><%= "What do you want to do today?" %></br></br>
<% if Income.last.present? %>
 Your last added income is: <span style="color:green"> 
     <%= "#{Income.last.try(:amount)} RON " %> </span> in 
     <%= "#{Income.last.try(:income_type).try(:name)}" %>
        <%= link_to "add new", new_income_path %> Income </br></br>
<% else %>
    You don't have any incomes added yet. You can
  <%= link_to "add new", new_income_path %> Incomes now. </br></br>
<% end %>

<% if Expense.last.present? %>
  Your last added expense is: <span style="color:red"> <%= "#{Expense.last.try(:amount)} RON " %></span> in 
  <%= "#{Expense.last.try(:expense_type).try(:name)}" %> -
  <%= link_to "add new", new_expense_path %> Expese </br></br>
<% else %>
  You don't have any expenses added yet. You can
  <%= link_to "add new", new_expense_path %> Expenses now. </br></br>
<% end %>

  Your balance for this month is: <span style="color:green"> 
  <%= "#{balance}" %> RON.</span>
  <%= link_to "See", "#" %> detailed information about your month so far.</span></p>
  <%= link_to "Invite a friend!", new_user_invitation_path %>
</h2>