<%= form_for :income_type, url: income_types_path do |f| %>
  <% if @income_type.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@income_type.errors.count, "error") %> prohibited
      this post from being saved:</h2>
    <ul>
    <% @income_type.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>

<%= form_for @income_type, :html => { :class => 'form-horizontal' } do |f| %>

    <%= f.label :name %>
    <%= f.text_field :name %>
 
    <%= f.label :parent_income_type_id %>
    <%= f.collection_select :parent_income_type_id, nil_parent_income_type, :id, :name, include_blank: true %>
    
  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                income_types_path, :class => 'btn' %>
  </div>
<% end %>
<% end %>